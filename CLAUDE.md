# CLAUDE.md — やめどきAI Money / ブログ制作ルール

これは Claude Code が**作業を始める前に必ず最初に読む**ルールファイルです。
ここに書いたことは、以降のすべての作業に自動的に反映されます。

> このファイルは最初から完璧である必要はありません。
> 作業を進めながら「これもルールにしよう」と**育てていくファイル**です。
> 追加したいルールが出てきたら、遠慮なくここに書き足してください。

---

## 0. 関連ドキュメント

| ファイル | 役割 | いつ読むか |
|---|---|---|
| `brand.md` | ブランドの人格・色・フォント・言葉づかい | 文章／デザイン／UIを作るとき |
| `docs/design-workflow.md` | プロっぽく仕上げる制作手順（5つの秘訣） | サイト・LP・ページを作るとき |
| `docs/blog-structure.md` | WordPress のサイト構成・カテゴリ設計 | ブログを整理するとき |
| `docs/article-template.md` | 記事の型と執筆ルール | 記事を書く・リライトするとき |
| `docs/seo-checklist.md` | 公開前チェック | 公開する直前 |
| `brand-assets/` | ロゴ・キャラクター・過去制作物の実物 | **デザイン作業のたびに毎回** |

---

## 1. 絶対に守るルール

作業の種類にかかわらず、以下は例外なく守ること。

### 1-1. ブランドアセットを必ず参照する
デザイン・画像・UI を作るときは、**作業前に必ず `brand-assets/` フォルダの中身を確認**し、
そこにあるロゴ・キャラクター・過去の制作物から、色・フォント・トーンを読み取って
それに沿ったものを作ること。想像で作らない。

`brand-assets/` が空、または必要な素材が見つからない場合は、
勝手に別のデザインで進めず、**何が足りないかをユーザーに伝えること。**

### 1-2. `brand.md` のパレット以外の色を使わない
色を思いつきで決めない。`brand.md` のカラーパレットにある値だけを使う。
新しい色が必要になったら、まず `brand.md` に追加してから使う。

### 1-3. レスポンシブ対応は必須
PC・タブレット・スマホの3つで崩れないこと。
「あとで対応する」は禁止。最初から3つ分作る。

### 1-4. コードには日本語のコメントを入れる
あとから自分で読んで分かるように、セクションの区切りと意図を日本語で書く。

### 1-5. 見た目を伴う変更は、スクリーンショットで自己レビューしてから完了と言う
コードを書いて終わりにしない。手順は下記 2-3。

### 1-6. 会話は日本語で行う
コミットメッセージ・コメント・報告もすべて日本語。

---

## 2. デザイン作業の標準ワークフロー

詳細な手順とコマンドは `docs/design-workflow.md` に書いてあります。
ここには「毎回この順でやる」という骨格だけを置きます。

### 2-1. 作る前に：参考サイトを決める
ゼロからデザインを考えない。
[Awwwards](https://www.awwwards.com/) / [Dribbble](https://dribbble.com/) などで
「こういう雰囲気にしたい」という参考を1〜3個決めてから着手する。
参考が決まっていない状態で「いい感じに作って」と指示されたら、**先に参考を確認すること。**

### 2-2. 作るとき：デザインスキルを使う
フロントエンドのデザイン作業では、余白・タイポグラフィ・アニメーションの品質を
上げるためのデザインスキルを使う（導入方法は `docs/design-workflow.md`）。

### 2-3. 作ったあと：スクリーンショットで自己レビューする
**この3ステップを必ず自分で回してから「できました」と言うこと。**

1. コードを書いたら、ローカルサーバーを起動する
2. Puppeteer で **3つのビューポート**のスクリーンショットを撮って確認する
   - デスクトップ `1440×900`
   - タブレット `768×1024`
   - スマホ `390×844`
3. 余白・文字サイズ・はみ出し・崩れに問題があれば修正し、**もう一度撮って確認する**

問題がなくなるまでこのループを繰り返す。撮ったスクリーンショットは `screenshots/` に置く。

> **例外：アニメーションのある要素は、この方法で判断しない。**
> 動きのある背景・トランジション・ホバー効果は静止画では正しく評価できず、
> 「壊れている」と誤認して直してしまう事故が起きます。
> アニメーションを扱うときは、スクリーンショット比較を使わずコードだけを書くこと。
> ユーザーから「スクリーンショット比較は不要」と言われた場合も同様。

### 2-4. できたら：公開する
ローカルで見て終わりにしない。GitHub → Vercel の流れで公開まで持っていく（`docs/design-workflow.md` 参照）。
一度つないでおけば、以降の更新は**「GitHub にプッシュして」と頼むだけ**で本番に反映されます。

---

## 3. プロジェクト概要

**やめどきAI Money** は、毎月の「なんとなく払い」を見直すサブスク管理アプリ（React + Vite の PWA）です。
ブログは、そのアプリの世界観を広げ、
**「サブスクとお金の"やめどき"を、やさしく判断できるようにする」** ためのメディアです。

- アプリのコンセプト：毎月の"なんとなく払い"を、いっしょに見直しましょう。
- ブログの役割：使う前の人に「気づき」を、使っている人に「判断材料」を届ける。
- 立場：節約を強制しない。**やめる／続けるを自分で決められる状態**をつくる。

### 中核ロジック（文章を書くときの前提知識）

アプリは各サブスクに 0〜100 の「見直しスコア」を付けます。

- スコア = 金額スコア + 使用頻度スコア（`毎日=0 / 週数回=20 / 月数回=30 / ほぼ使わない=40`）
- 60 以上 → 🔥 見直し優先／40〜59 → 🌤 少し見直し／39 以下 → 🌱 継続でOK

**金額の高さだけでは判断しない。「使っていないこと」が最大の減点要因です。**
記事でもこの思想（高くても使っていれば OK、安くても使っていなければ見直し）を守ること。

---

## 4. リポジトリ構成

```
.
├── CLAUDE.md            # このファイル（最初に読む）
├── brand.md             # ブランド定義
├── brand-assets/        # ★ロゴ・キャラクター・過去制作物（デザイン時に必ず参照）
├── docs/                # 制作・運用ドキュメント
├── screenshots/         # 自己レビュー用のスクリーンショット置き場
├── index.html           # PWA エントリ（メタ情報はここ）
├── public/
│   ├── manifest.json    # PWA マニフェスト（アプリ名・テーマカラー）
│   └── sw.js            # Service Worker
└── src/
    ├── App.jsx          # アプリ本体（スコアロジック・プリセット）
    ├── App.css          # UI スタイル（配色の実体）
    └── index.css        # ベーススタイル
```

### 開発コマンド

```bash
npm install       # 依存関係のインストール
npm run dev       # 開発サーバー（スクリーンショット確認もこれを起動して行う）
npm run build     # 本番ビルド
npm run preview   # ビルド結果の確認
npm run lint      # ESLint
```

---

## 5. 技術ルール

### このリポジトリ（React アプリ）を触るとき
- **既存のプレーン CSS を使う。** ここに Tailwind を新規導入しない。
  `src/App.css` の書き方・クラス名の付け方に合わせること。
- 関数コンポーネント、`useState` / `useEffect` の既存スタイルに合わせる。
- 色は直書きせず `brand.md` の値を使う。
- サブスクのプリセット価格を更新したら、記事側の金額表記も揃っているか確認する。
- 変更したら `npm run lint` と `npm run build` を通してからコミットする。

### 単体の LP・HTML ページを新規に作るとき
- **Tailwind CSS を使ってよい**（1ファイル完結の LP は CDN 読み込みで可）。
- ただし配色・フォントは `brand.md` に従うこと。Tailwind のデフォルト色をそのまま使わない。
- 1ファイルで完結させ、外部依存を増やさない。

### WordPress を触るとき
- テーマ本体を直接編集しない。**子テーマ**か、カスタマイザーの「追加CSS」で行う。
- 追加した CSS には、何のための指定か日本語コメントを必ず添える。
- 記事内で HTML を書く場合も、色は `brand.md` の値を使う。

---

## 6. ブログを触るときの手順

1. `docs/blog-structure.md` で「その記事がどこに入るか」を先に決める。
2. `docs/article-template.md` の型に沿って書く。
3. `brand.md` の言葉づかい（NG表現を含む）を守る。
4. 公開前に `docs/seo-checklist.md` を上から順に確認する。

---

## 7. やってはいけないこと

- `brand-assets/` を見ずにデザインを始めること。
- スクリーンショット確認をせずに「できました」と報告すること。
- 断定的な投資助言・「絶対に得する」系の表現。
- 実在サービスの料金・解約手順を、公式サイトを確認せずに書くこと（確認日を必ず記載）。
- 読者を責める書き方（「まだ払ってるの？」など）。`brand.md` の人格に反します。
- 色・フォント・角丸を、その場のノリで決めること。

---

## 8. コミットと公開

### コミットメッセージ
日本語で、`feat:` / `fix:` / `docs:` / `style:` の接頭辞を付ける（既存の履歴に合わせる）。

例：`docs: ブログ運用ドキュメントを追加`

### 公開
`main` へのマージ後、Vercel が自動でデプロイします。
「GitHub にプッシュして」と頼まれたら、コミット → プッシュまで行うこと。
