# CLAUDE.md — やめどきAI Money プロジェクト指針

このファイルは Claude Code がこのリポジトリと、連動する WordPress ブログを
扱うときの「司令塔」です。作業を始める前に必ずここを読み、
必要に応じて下記の関連ドキュメントを参照してください。

## 関連ドキュメント

| ファイル | 役割 | いつ読むか |
|---|---|---|
| `brand.md` | ブランドの人格・色・言葉づかいの定義 | 文章／デザイン／UIコピーを書くとき |
| `docs/blog-structure.md` | WordPress のサイト構成・カテゴリ・固定ページ | ブログを整理・設計するとき |
| `docs/article-template.md` | 記事の型と執筆ルール | 記事を書く・リライトするとき |
| `docs/seo-checklist.md` | 公開前の必須チェック | 記事を公開する直前 |

---

## 1. プロジェクト概要

**やめどきAI Money** は、毎月の「なんとなく払い」を見直すためのサブスク管理
アプリ（React + Vite の PWA）です。
そしてこのブログは、そのアプリの世界観を広げ、
**「サブスクとお金の"やめどき"を、やさしく判断できるようにする」** ためのメディアです。

- アプリのコンセプト：毎月の"なんとなく払い"を、いっしょに見直しましょう。
- ブログの役割：アプリを使う前の人に「気づき」を、使っている人に「判断材料」を届ける。
- 立場：節約を強制しない。**やめる／続けるを自分で決められる状態**をつくる。

### プロダクトの中核ロジック（記事を書くときの前提知識）

アプリは各サブスクに 0〜100 の「見直しスコア」を付けます。

- スコア = 金額スコア + 使用頻度スコア（`毎日=0 / 週数回=20 / 月数回=30 / ほぼ使わない=40`）
- 60 以上 → 🔥 見直し優先
- 40〜59 → 🌤 少し見直し
- 39 以下 → 🌱 継続でOK

**重要：金額の高さだけでは判断しない。「使っていないこと」が最大の減点要因。**
記事でも必ずこの思想（＝高くても使っていれば OK、安くても使っていなければ見直し）を守ること。

---

## 2. リポジトリ構成

```
.
├── CLAUDE.md            # このファイル
├── brand.md             # ブランド定義
├── docs/                # ブログ運用ドキュメント
├── index.html           # PWA エントリ（メタ情報はここ）
├── public/
│   ├── manifest.json    # PWA マニフェスト（アプリ名・テーマカラー）
│   └── sw.js            # Service Worker
└── src/
    ├── App.jsx          # アプリ本体（スコアロジック・プリセット）
    ├── App.css          # UI スタイル（配色の実体）
    └── index.css        # ベーススタイル
```

### 開発コマンド

```bash
npm install       # 依存関係のインストール
npm run dev       # 開発サーバー
npm run build     # 本番ビルド
npm run preview   # ビルド結果の確認
npm run lint      # ESLint
```

---

## 3. 作業のときのルール

### コードを触るとき
- 既存のコードスタイル（関数コンポーネント、`useState`/`useEffect`、日本語コメント）に合わせる。
- 色を直書きせず、`brand.md` のパレットにある値を使う。
- サブスクのプリセット価格を更新したら、記事側の金額表記も揃っているか確認する。
- 変更したら `npm run lint` と `npm run build` を通してからコミットする。

### ブログを触るとき
1. `docs/blog-structure.md` で「その記事がどこに入るか」を先に決める。
2. `docs/article-template.md` の型に沿って書く。
3. `brand.md` の言葉づかい（NG表現を含む）を守る。
4. 公開前に `docs/seo-checklist.md` を上から順に確認する。

### やってはいけないこと
- 断定的な投資助言・「絶対に得する」系の表現。
- 実在サービスの解約方法を、公式情報を確認せずに書くこと（料金・手順は必ず一次情報を確認し、確認日を記載）。
- 読者を責める書き方（「まだ払ってるの？」など）。`brand.md` の人格に反します。

### コミットメッセージ
日本語で、`feat:` / `fix:` / `docs:` / `style:` の接頭辞を付ける（既存の履歴に合わせる）。

例：`docs: ブログ運用ドキュメントを追加`

---

## 4. 動画ノウハウの取り込み状況

> **未取り込み**：指定された YouTube 動画
> （`https://www.youtube.com/watch?v=hLMi8syJxMg`）は、
> 現在の実行環境のネットワークポリシーで `www.youtube.com` へのアクセスが
> ブロックされているため、内容を読み取れていません。
>
> 文字起こし・要約を受け取り次第、この節を「動画から学んだ運用ルール」に
> 置き換え、`docs/` 配下の各ドキュメントへ反映します。
> それまでの内容は、実際のアプリのコンセプト・配色・ロジックと、
> 一般的なブログ運用のベストプラクティスに基づく暫定版です。
